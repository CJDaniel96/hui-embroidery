# 開發環境用的 Dockerfile
FROM node:20-alpine

# 設定工作目錄
WORKDIR /app

# 複製 package 相關檔案
COPY package*.json ./

# 安裝所有依賴（包含開發依賴）
RUN npm install

# 複製專案檔案
COPY . .

# 暴露埠號
EXPOSE 3000

# 開發模式啟動指令
CMD ["npm", "run", "dev"]